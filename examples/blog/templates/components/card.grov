{% props title, summary, href="#", date="" %}
<article class="card">
  <h2>
    <a href="{{ href }}">{{ title }}</a>
  </h2>
  {% if date %}<time>{{ date }}</time>{% endif %}
  <p>{{ summary | truncate(120) }}</p>
  <div class="flex-row">
    {% slot "tags" %}{% endslot %}
  </div>
</article>
